# Copy to .env if you run without Docker, or keep as reference
DB_HOST=127.0.0.1
DB_NAME=entertainment_platform
DB_USER=p3user
DB_PASS=p3pass
ENVIRONMENT=development

# Optional mail/payment/etc.
SMTP_HOST=localhost
SMTP_PORT=1025
